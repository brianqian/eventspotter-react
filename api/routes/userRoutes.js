const router = require('express').Router();
const userController = require('../controllers/userController');

// router
//   .route('/info')
//   .get(userController.saveUserLifts)
//   .post(userController.updateUserInfo);

module.exports = router;
