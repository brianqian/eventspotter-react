const router = require('express').Router();
const accessoryController = require('../controllers/libraryController');

// router.route('/add').post(accessoryController.addLibrary);

module.exports = router;
